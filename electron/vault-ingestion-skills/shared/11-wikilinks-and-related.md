# Wikilinks and Related

- Every note must have `## Related` with concrete `[[wikilinks]]` to related notes.
- Use paths relative to vault root without file extension in links when following Obsidian convention.
- Cross-link new notes to parent overviews and sibling concepts.
- No implicit references; every link must point to a real planned or existing path.
